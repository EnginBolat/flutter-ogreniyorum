import 'package:flutter/material.dart';

import 'multi_thread_view.dart';

class MultiThread extends StatefulWidget {
  const MultiThread({Key key}) : super(key: key);

  @override
  State<MultiThread> createState() => MultiThreadView();
}
